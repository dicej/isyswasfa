[workspace]
resolver = "2"
members = [
  "echo",
  "hash-all",
  "middleware",
  "service",
  "round-trip"
]

[workspace.dependencies]
async-trait = "0.1.77"
wit-bindgen = { path = "../../wit-bindgen/crates/guest-rust" }
isyswasfa-guest = { path = "../../guest" }
